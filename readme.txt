1) test an image :
    imread(gray_scale) --> resize (to 50)  -->  turn it to array 
    -->  feed it to network as .view(-1,1,50,50) --> play with the result :) ( softmax ,argmax ..) --> get ur result dog or cat